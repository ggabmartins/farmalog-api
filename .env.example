# Copie para .env (nao versionado) antes de subir o compose:
#   cp .env.example .env
# Valores abaixo sao ficticios e servem apenas para desenvolvimento local.

POSTGRES_DB=farmalog
POSTGRES_USER=farmalog
POSTGRES_PASSWORD=troque_em_local
POSTGRES_PORT=5432
